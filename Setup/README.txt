These are the setup files which need to be run before starting the file

Ubuntu version needs to be above 16.10 though there is no guarantee that it will work on the unstable versions of ubuntu.
Ubuntu:

cd to UbuntuInstall.sh
run: sudo ./UbuntuInstall.sh


MacOS version is not yet implemented

Windows version is not yet implemented
